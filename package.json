{
  "name": "parameters-js",
  "version": "1.0.4",
  "description": "Simply manage url parameters.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "doc": "jsdoc2md index.js --template README.hbs --partial partials/*.hbs > README.md",
    "update-doc": "npm run doc;git add .;git commit -m 'Updated README.md';git push",
    "commit": "npm run doc;git add .;git commit;git push",
    "release": "npm run doc;git add .;git commit;git push;npm publish"
  },
  "repository": {
    "type": "git",
    "url": "https://www.github.com/juliendargelos/parameters-js"
  },
  "keywords": [
    "parameters",
    "http",
    "data",
    "form",
    "formdata",
    "inputs"
  ],
  "author": "Julien Dargelos",
  "license": "MIT",
  "devDependencies": {
    "jsdoc-to-markdown": "^3.0.2"
  }
}
